---
import { LinkCard } from '@astrojs/starlight/components'

export type ShowcaseLinkProps = Props

interface Props {
  description?: string
  href: string
  name: string
}

const { description, href, name } = Astro.props
---

<LinkCard {href} title={name} {description} />
